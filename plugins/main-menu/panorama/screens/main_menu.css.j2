{% import "@main-menu/meatgg/menu_screen.css.j2" as meatgg %}
{% from "main_menu/icons.j2" import ICONS %}
{{ meatgg.menu_styles(ICONS) }}

.wordmark {
  width: 100%;
  height: 36px;
  flow-children: right;
}

.wordmark__mark {
  width: 36px;
  height: 36px;
  margin-right: 10px;
}

.wordmark__meat,
.wordmark__gg {
  vertical-align: center;
  font-size: 22px;
  font-weight: black;
  letter-spacing: -1px;
}

.wordmark__meat {
  color: text-primary;
}

.wordmark__gg {
  color: primary;
}

.hero {
  width: 100%;
  height: fit-children;
  flow-children: right;
  padding: 24px;
  border-radius: 14px;
  background-color: gradient(linear, 0% 0%, 0% 100%, from(card-top), to(window-bottom));
  border: 1px solid hairline;
}

.hero__mark {
  width: 72px;
  height: 72px;
  margin-right: 20px;
  vertical-align: center;
}

.hero__text {
  width: fill-parent-flow(1);
  height: fit-children;
  flow-children: down;
  vertical-align: center;
}

.hero__overline {
  color: primary;
}

.hero__title {
  width: 100%;
  margin-top: 6px;
  font-size: 22px;
  font-weight: bold;
  letter-spacing: -0.3px;
  color: text-primary;
}

.hero__body {
  width: 100%;
  margin-top: 8px;
  font-size: 14px;
  color: text-secondary;
}

.home-actions {
  width: 100%;
  height: fit-children;
  flow-children: right;
  margin-top: 16px;
}

.home-actions .button {
  width: fit-children;
  height: 44px;
  padding: 0px 24px;
}
