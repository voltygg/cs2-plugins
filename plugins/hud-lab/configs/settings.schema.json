{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "hud-lab settings.jsonc",
  "description": "Runtime configuration for the hud-lab plugin. Extend alongside the Settings struct in src/Config.hpp - missing keys fall back to their C++ defaults.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "$schema": { "type": "string" },
    "plugin": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "locale": {
          "type": "string",
          "default": "en",
          "description": "Translation language; matches a file in configs/translations."
        }
      }
    },
    "hud": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "layout": {
          "type": "string",
          "default": "hud_lab",
          "description": "Layout hudlab_spawn uses with no argument. A bare name is expanded to panorama/layout/custom_game/<name>.xml."
        }
      }
    }
  }
}
