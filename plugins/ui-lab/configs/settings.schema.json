{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ui-lab settings.jsonc",
  "description": "Runtime configuration for the ui-lab plugin. Extend alongside the Settings struct in src/Config.hpp - missing keys fall back to their C++ defaults.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "$schema": { "type": "string" },
    "plugin": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "locale": {
          "type": "string",
          "default": "en",
          "description": "Translation language; matches a file in configs/translations."
        }
      }
    },
    "ui": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "layout": {
          "type": "string",
          "default": "ui_lab",
          "description": "Layout uilab_spawn uses with no argument. A bare name is expanded to panorama/layout/custom_game/<name>.xml."
        }
      }
    }
  }
}
