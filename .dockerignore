# The image build only needs the Conan manifest and lock - profiles come from
# `conan config install`, the runtime stage COPYs nothing, and local builds
# volume-mount the source. This whitelist keeps ~1.4GB out of the context.
*
!conanfile.py
!conan.lock
