{
  "$schema": "./schemas/overrides.schema.json",
  "command_overrides": {
    "kick": "cd",
    "restart": "g"
  },
  "group_overrides": {
    "@punish": "d"
  },
  "notes": {
    "command_overrides": "Override default flags required for specific commands",
    "group_overrides": "Define command groups for bulk overrides"
  }
}
