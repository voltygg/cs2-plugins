services:
  build:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
      target: build
    working_dir: /app
    volumes:
      - ..:/app
    command: bash -lc 'CC=gcc-14 CXX=g++-14 python3 scripts/build.py linux-steamrt-release'
