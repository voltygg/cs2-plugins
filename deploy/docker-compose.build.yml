services:
  build:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
      target: build
    working_dir: /app
    volumes:
      - ..:/app
    command: bash -lc 'CC=gcc-14 CXX=g++-14 voltmod build linux-steamrt-release && voltmod test linux-steamrt-release'
