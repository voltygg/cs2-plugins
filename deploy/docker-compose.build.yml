services:
  build:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
      target: build
    working_dir: /app
    volumes:
      - ..:/app
    command: bash -lc 'CC=gcc-14 CXX=g++-14 bash scripts/build.sh linux-steamrt-release'
