# CI: store this file's contents in the environment's SERVER_ENV secret.

PANEL_API_KEY=
DB_HOST=
DB_PASSWORD=
CHEAT_API_KEY=

# The RCON password from the panel's Startup tab; empty skips the plugin load check.
RCON_main=
